import { Profile } from './Social-profile';

export const App = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Profile />
    </div>
  );
};
